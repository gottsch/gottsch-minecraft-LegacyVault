# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx4G

minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.2.0
forge_version_range=[47,)
loader_version_range=[47,)
mapping_channel=official
mapping_version=1.20.1

mod_id=legacyvault
mod_name=Legacy Vault
mod_license=GNU GPL v3
mod_version=3.0.0
mod_group_id=gottsch
mod_authors=gottsch
mod_description=Legacy Vault is a mod that allows a player(s) to store inventory between worlds. It is like an Ender Chest but instead of a single inventory shared between multiple chests, it is a single inventory shared between multiple chests AND multiple worlds.
